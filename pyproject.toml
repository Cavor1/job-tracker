[project]
name = "job-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "prompt-toolkit>=3.0.52",
    "sqlalchemy>=2.0.50",
]

[tool.uv]
exclude-newer = "7 days"
